  //const {title,description,code,price,status,stock,category,thumbnails} = request.body;
   //const result =await productModel.create({title,description,code,price,status,stock,category,thumbnails});
 //res.json({status:'success',payload: result})
    //const productoGenerado = new productModel(productoNew) //genera un nuevo modelo
    //await productoGenerado.save();
    //o asi
    // const productos = await productModel.find().lean().exec()
    // req.io.emit('updateProducts',productos)
     //es como create
    //console.log(req.body);
    //payload convencion significa datos
    //res.status(201).json({status:'success',payload:productoGenerado})
    // const productNuevo= {title,description,code,price,stock,category,thumbnails};
    